import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#56E39F',
      contrastText: '#FFFFFF',
    },
    secondary: {
      main: '#1D283E',
      contrastText: '#FFFFFF',
    },
  },
});

export default theme;
